//
// GENERATED by CMake from tests/exampleFixtures.cmake through
// tests/exampleFixtures.h.in. Do not edit: edit the table in the .cmake
// file, which is also what builds the example_parity_* ctest entries, so the
// suite that walks this array and the entries that run rigExecPose can never
// disagree about which frames an example is tested at or which prim a drag
// lands on.
//
#ifndef RIGEXEC_EXAMPLE_FIXTURES_H
#define RIGEXEC_EXAMPLE_FIXTURES_H

/// One shipped example stage and how the harnesses exercise it.
struct RigExecExampleFixture {
    /// Stage path relative to the examples directory.
    const char *stage;
    /// Comma-separated frame list: every authored sample time plus points
    /// between them, so an interpolated frame is compared and not only the
    /// held keys.
    const char *frames;
    /// A control prim a gizmo could grab, or "" when the example authors no
    /// controls at all.
    const char *controlPrim;
    /// The avar on \p controlPrim to drag; double-valued.
    const char *controlAvar;
    /// A mover, weight or constraint prim: the other kind of drag, the one
    /// that lands on an operator's own input.
    const char *operatorPrim;
    /// The attribute on \p operatorPrim to drag; float- or double-valued.
    const char *operatorInput;
    /// Whether RigExecBakedProgram::Build accepts this rig today. A suite
    /// that walks the table skips the rest until the operator group lands.
    bool bakesToday;
    /// When it does not bake, the operator group whose refusal IsBakeable
    /// reports first; "" when it bakes.
    const char *blockedBy;
};

static const RigExecExampleFixture kRigExecExampleFixtures[] = {
@RIGEXEC_EXAMPLE_FIXTURE_ROWS@};

#endif  // RIGEXEC_EXAMPLE_FIXTURES_H
